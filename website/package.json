{
  "name": "open-rando-website",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "predev": "rsync -a --delete --exclude='*.pmtiles' ~/.local/share/open-rando/data/ public/data/ 2>/dev/null || echo 'No data/ found'",
    "dev": "astro dev",
    "prebuild": "rsync -a --delete --exclude='*.pmtiles' ~/.local/share/open-rando/data/ public/data/",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "bash deploy.sh",
    "test": "bun test"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@astrojs/tailwind": "^6.0.0",
    "astro": "^5.0.0",
    "maplibre-gl": "^5.23.0",
    "pmtiles": "^4.4.1",
    "qrcode-generator": "^1.4.4",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "typescript": "^6.0.2"
  }
}
