{
  "name": "open-rando-website",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "predev": "cp -r ../data/ public/data/ 2>/dev/null || echo 'No data/ found'",
    "dev": "astro dev",
    "prebuild": "cp -r ../data/ public/data/",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/tailwind": "^6.0.0",
    "tailwindcss": "^3.4.0",
    "leaflet": "^1.9.0"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.0"
  }
}
