---
import type { Hike } from '../lib/catalog';
import HikeCard from './HikeCard.astro';

interface Props {
  hikes: Hike[];
}

const { hikes } = Astro.props;
---

<div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
  {hikes.map((hike) => <HikeCard hike={hike} />)}
</div>
